<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
                             http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- Identificação do Projeto -->
    <groupId>dev.ProjetoEDA</groupId> <!-- Seu groupId -->
    <artifactId>Projeto-EDA-ComparacaoEstruturasJava</artifactId> <!-- Nome do artefato -->
    <version>1.0-SNAPSHOT</version> <!-- Versão do seu projeto -->
    <packaging>jar</packaging> <!-- Tipo de empacotamento (JAR) -->

    <!-- Configuração de Build -->
    <build>
        <sourceDirectory>src/main/java</sourceDirectory> <!-- Diretório de código-fonte -->
        <testSourceDirectory>src/test/java</testSourceDirectory> <!-- Diretório de testes -->

        <plugins>
            <!-- Plugin para compilar o código Java -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source> <!-- Versão do Java -->
                    <target>1.8</target> <!-- Versão do Java -->
                </configuration>
            </plugin>
            <!-- Plugin para gerar o JAR -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-jar-plugin</artifactId>
                <version>3.2.0</version>
                <configuration>
                    <archive>
                        <manifestEntries>
                            <Main-Class>dev.ProjetoEda.App</Main-Class> <!-- Classe principal do projeto -->
                        </manifestEntries>
                    </archive>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <!-- Dependências do Projeto -->
    <dependencies>
        <!-- Dependência do org.json para manipulação de JSON -->
        <dependency>
            <groupId>org.json</groupId>
            <artifactId>json</artifactId>
            <version>20240205</version>
        </dependency>

        <!-- Dependência do JUnit para testes -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>

        <!-- Dependência do Log4j para logs -->
        <dependency>
            <groupId>org.apache.logging.log4j</groupId>
            <artifactId>log4j-core</artifactId>
            <version>2.21.0</version>
        </dependency>
    </dependencies>

    <!-- Profiles para diferentes classes -->
    <profiles>
        <!-- Profile para executar a classe App.java -->
        <profile>
            <id>app</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <exec.mainClass>dev.ProjetoEDA.App</exec.mainClass>
            </properties>
        </profile>

        <!-- Profile para executar BenchArrayListInsertion.java -->
        <profile>
            <id>bench</id>
            <properties>
                <exec.mainClass>dev.ProjetoEDA.bench.BenchArrayListInsertion</exec.mainClass>
            </properties>
        </profile>
    </profiles>

</project>
